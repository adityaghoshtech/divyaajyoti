import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';import {articles} from '@/lib/data';
export default function Insights(){return <main><section className="pagehero"><div className="container"><div className="eyebrow">The journal</div><h1>Ideas for navigating the next step.</h1><p>Property, learning, design, astrology and the practical questions that sit between them.</p></div></section><section className="section"><div className="container"><div className="grid3">{articles.map(a=><Link href={`/insights/${a.slug}`} className="article" key={a.slug}><div className="article-img" style={{backgroundImage:`url(${a.image})`}}/><div className="article-body"><div className="eyebrow">{a.category} · {a.date}</div><h3>{a.title}</h3><p>{a.excerpt}</p><span className="btn btn-light btn-sm">Read article <ArrowUpRight size={13}/></span></div></Link>)}</div></div></section></main>}
