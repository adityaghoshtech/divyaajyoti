import {notFound} from 'next/navigation';import Link from 'next/link';import {ArrowLeft,ArrowUpRight} from 'lucide-react';import {articles} from '@/lib/data';
export default async function Article({params}:{params:{slug:string}}){const a=articles.find(x=>x.slug===params.slug);if(!a)return notFound();return <main className="detail"><div className="container" style={{maxWidth:980}}><Link href="/insights" className="muted" style={{fontSize:12,display:'inline-flex',gap:6,alignItems:'center',marginBottom:25}}><ArrowLeft size={13}/> The journal</Link><div className="eyebrow">{a.category} · {a.date}</div><h1 className="display" style={{fontSize:'clamp(45px,7vw,76px)',lineHeight:1.02,maxWidth:900}}>{a.title}</h1><div className="hero-img" style={{height:480,marginTop:30,borderRadius:25,backgroundImage:`url(${a.image})`,backgroundSize:'cover',backgroundPosition:'center'}}/><article style={{maxWidth:760,margin:'45px auto',fontSize:16,lineHeight:1.9,color:'#526175'}}><p>{a.excerpt}</p><h2 className="display" style={{fontSize:34,color:'var(--ink)'}}>Start with the question, not the answer.</h2><p>When a decision matters, the fastest route is rarely the most useful one. Begin by defining what you are actually trying to solve, then gather only the information that changes that decision.</p><p>Whether you are comparing homes, choosing a learning path or using a reflective practice, context makes the difference between more information and better understanding.</p><Link href="/contact" className="btn btn-dark">Continue the conversation <ArrowUpRight size={14}/></Link></article></div></main>}
